<% if profile.image.url =~ /system/ %>
  <%= link_to(image_tag(profile.image.url(:small)), profile_path(profile)) %>
<% else %>
  <%= link_to(image_tag("Laina.jpg"), profile_path(profile)) %>
<% end %>